import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const Loginexamples = () => {
    return (
        <View>
            <Text style={styles.login}>Hello World</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    login: {
        marginBottom: 20,
    }
})

export default Loginexamples;
